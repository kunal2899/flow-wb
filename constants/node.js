const NODE_TYPE = {
  ACTION: "action",
  CONDITION: "condition",
  DELAY: "delay",
};

const ON_ERROR_ACTION = {
  CONTINUE: "continue",
  STOP: "stop",
  RETRY: "retry",
};

module.exports = {
  NODE_TYPE,
  ON_ERROR_ACTION,
};
